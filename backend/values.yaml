replicaCount: 1

image:
  repository: ""  
  tag: "latest"
  pullPolicy: IfNotPresent

imagePullSecrets: []

service:
  type: LoadBalancer
  port: 3001
serviceAccount:
  create: false

ingress:
  enabled: false

autoscaling:
  enabled: false

